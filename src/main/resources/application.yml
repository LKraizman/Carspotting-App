server:
  port: 5000
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: chkyoslf
    password: pass123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true
  main:
    web-application-type: servlet
    allow-bean-definition-overriding: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: spotitapplication@gmail.com
    password: cvlpuwtrqjpucwql
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
springdoc:
  swagger-ui:
    path: /spotit-documentation
    try-it-out-enabled: true
    operations-sorter: method
    enabled: true
  api-docs:
    enabled: true
    path: /spotit-api-docs
carspotting:
  openapi:
    url: http://localhost:5000
